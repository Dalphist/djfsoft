<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.stock.StockMapper">
	<update id="stockOut" parameterType="StockOutInfo">
		update t_stock set normal_quantity = normal_quantity - #{normalQuantity}
		where product_id=#{id}
	</update>
	<update id="stockIn" parameterType="StockInInfo">
		update t_stock set normal_quantity = normal_quantity + #{normalQuantity}
		where product_id=#{id}
	</update>
	<select id="getStockList" resultType="StockInfo">
		select stock.* 
		from t_stock stock
	</select>
</mapper>